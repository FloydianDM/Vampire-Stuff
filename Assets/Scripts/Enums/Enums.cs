public enum TypeEnum
{
    Fireable,
    Usable,
    Enemy,
    EnemyDeathEffect,
}

public enum GameState
{
    Play,
    PauseMenu
}

public enum ChestState
{
    Closed,
    HealthItem,
    WeaponItem,
    XP
}

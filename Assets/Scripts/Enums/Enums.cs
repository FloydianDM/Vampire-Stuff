public enum TypeEnum
{
    Fireable,
    Enemy,
    EnemyDeathEffect,
}

public enum GameState
{
    Play,
    PauseMenu
}

public enum ChestState
{
    Closed,
    HealthItem,
    WeaponItem,
    XP
}
